name: issue873
on:
  workflow_dispatch:
jobs:
  test_frontend:
    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v3

    - name: Use Node.js
      uses: actions/setup-node@v3
      with:
        node-version: 19.x

    
    
