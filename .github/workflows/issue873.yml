name: issue873
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: macos-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4


      - name: Set up Node.js
        uses: gowridurgad/setup-node@v4
        with:
          node-version: 'latest'
          cache: yarn

    
    
